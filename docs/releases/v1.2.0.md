## Summary
Minor release focused on multi-user wallet hardening and transfer integrity controls.

## Included
- enforced sender wallet token validation for transfer operations.
- added per-wallet nonce validation to prevent replay-style transfer reuse.
- introduced hash-chain transfer integrity checks and CLI verification command.
- extended multi-user CLI workflow with stronger validation options.

## Validation
- multi-user wallet domain tests passed.
- CLI tests passed for nonce and integrity verification flows.
- promotion chain and release process completed under GitFlow policy.

## Next
- continue with dual-model consolidation (`ACCOUNT` + `UTXO`) under the planned `v2.0.0` phase.
- expand interactive terminal access and end-to-end multi-user scenarios.
