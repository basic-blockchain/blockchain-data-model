name: CI Pull Request

on:
  pull_request:
    branches: ["main", "develop"]

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/reusable-ci-polyglot.yml
    with:
      language: python
      python-version: "3.12"
      run-lint-command: python -m py_compile account-model.py utxo-model.py
      run-tests-command: pytest -q
